{
  "name": "biblio-num",
  "version": "1.0.1",
  "description": "Projet biblio-num",
  "directories": {
    "doc": "docs"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:server": "turbo dev --filter=server",
    "dev:server-in-docker": "turbo dev:in-docker --filter=server",
    "test": "run-s docker:db:up test:only docker:db:down",
    "test:only": "turbo test",
    "prepare": "husky install",
    "lint": "turbo lint",
    "docker:db:up": "docker compose --profile db-only -f apps/server/docker-compose.dev.yml up -d",
    "docker:db:down": "docker compose --profile db-only -f apps/server/docker-compose.dev.yml down",
    "build:client": "turbo build --filter=client",
    "lint:client": "turbo lint --filter=client",
    "test:client": "turbo test --filter=client",
    "build:server": "turbo build --filter=server",
    "lint:server": "turbo lint --filter=server",
    "test:server": "turbo test --filter=server",
    "docker:dev": "docker compose --profile dev -f apps/server/docker-compose.dev.yml up -d",
    "docker:prod:build": "docker compose -f docker-compose.prod.yml build",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d",
    "docker:prod:stop": "docker compose -f docker-compose.prod.yml down"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LAB-MI/biblio-num.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/LAB-MI/biblio-num/issues"
  },
  "homepage": "https://github.com/LAB-MI/biblio-num#readme",
  "devDependencies": {
    "husky": "8.0.2"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "turbo": "1.9.3"
  }
}
